# Deferred Work

This file tracks issues surfaced during code review that are real but not actionable in the story under review. Each entry cites the source review and notes the planned resolution path (later story, architecture doc cleanup, etc.).

## Deferred from: code review of story-1.1 (2026-05-09)

- **`README.md` listed in `package.json` `files` whitelist but file does not exist.** `npm pack`/`npm publish` will warn rather than error, and AC3 of Story 1.1 mandates this exact `files` array. Resolution path: Story 5-4 (documentation deliverables) creates README.md.
- **Architecture doc inconsistency: `eslint-plugin-cmemmov/` vs `eslint-rules/`.** Architecture §"Code Quality" line ~352 mentions `eslint-plugin-cmemmov/`, while the canonical project tree (line ~523) uses `eslint-rules/`. Story 1.1 followed the canonical tree (correct). Resolution path: architecture doc edit to remove the stale `eslint-plugin-cmemmov/` reference.
- **`ci-seed.yml` will be replaced by `ci.yml` in Story 1.13.** The story spec explicitly notes this supersession. Resolution path: Story 1.13 (full 3-OS test matrix) deletes/renames `ci-seed.yml` and introduces `ci.yml` with the complete `npm run check` matrix.
- **Architecture doc init-step snippet (lines ~128-148) predates the v22 Node floor and Story 1.1's documented spec deviations.** Specifically, the architecture's stack-init commands and `tsup.config.ts` / `eslint.config.js` snippets contain the same internal contradictions that the dev had to resolve. Resolution path: architect ratifies the dev's deviations (`tsconfig.eslint.json` addition + `tsup` `entry` object form) and updates the architecture doc so subsequent stories' "exact content" references stay in sync.

## Deferred from: code review of story-1.2 (2026-05-09)

All findings below are real enforcement gaps but were intentionally deferred because the Story 1.2 spec mandated *verbatim* "exact content" for each rule file; patching scope expansion in-place would violate the spec contract. Each item should be reconsidered as part of a future hardening story (candidate: a "rule hardening" story under Epic 1 cleanup, or rolled into the first story that touches each rule's call sites).

- **MEDIUM — `no-process-env-home` misses destructuring and computed-env access.** Bypass patterns: `const { HOME } = process.env;` walks past the `MemberExpression` visitor; `process['env'].HOME` is rejected because the rule requires `node.object.property.type === 'Identifier'` (computed access yields a `Literal`). Resolution path: add `VariableDeclarator` + `ObjectPattern` walker for destructuring and relax the env-access type check to accept the `'env'` `Literal` form. Add tests for both bypass patterns.
- **MEDIUM — `no-console-outside-output` does not block `console.info/debug/trace/table/dir`.** AC3 lists exactly `log/error/warn` plus the two `process.std*.write` forms, so the rule correctly matches AC text — but the architectural intent is that ALL stdout/stderr writes route through the Output module. Resolution path: expand `BANNED_IDENTIFIERS` to cover the full ban list, update AC text in a follow-up story, add tests.
- **LOW — `no-raw-json-parse` does not catch `JSON['parse'](text)` (computed property access).** Theoretical bypass; would only matter if a contributor deliberately works around the rule. Resolution path: relax the `callee.property.type === 'Identifier'` constraint to also accept a `'parse'` string `Literal`. Add a regression test.
- **LOW — `no-hardcoded-separator` does not catch template-literal separators (`` `${a}/${b}` ``).** Spec literally says "string literal whose value is exactly `'/'`", so template strings are out of scope by design. Resolution path: extend rule to walk `TemplateElement` cooked values when the next holes look path-shaped, or accept the gap as an acknowledged limitation in the rule's docs. Best paired with adoption of the path engine (Story 1.7+) so the gap is closed by call-site discipline rather than the lint rule.

## Deferred from: code review of story-1.3 (2026-05-09)

- **LOW — `SupportedPlatform` type exported but unused.** `src/core/path-engine.types.ts` exports `SupportedPlatform = Extract<NodeJS.Platform, 'win32' | 'darwin' | 'linux'>` per Story 1.3 Task 1, but `slugToPath` accepts `NodeJS.Platform` (broader) and uses a structural null guard rather than narrowing at the type boundary. The story spec mandated the export, so removing it would violate the spec; using it as the parameter type would deviate from the Dev Notes signature ("`slug: string, sourcePlatform: NodeJS.Platform`"). Resolution path: spec clarification in Story 2.1 (cross-OS) — when `suggestRemap` lands and `slugToPath`/`suggestRemap` callers are wired up, decide whether the public surface narrows to `SupportedPlatform` or remains `NodeJS.Platform` with runtime null guards. Either choice is defensible; the inconsistency should be reconciled then.
- **LOW — `tsconfig.json: resolveJsonModule: true` change not pre-listed in Story 1.3's "Project Structure Notes".** The story listed only `vitest.config.ts` under "Files being modified". The dev added `resolveJsonModule: true` to `tsconfig.json` to satisfy AC 6 (fixture loaded via `import … with { type: 'json' }` must typecheck under `tsc --noEmit`). The change is one line, additive, non-breaking, and documented in the story's Dev Agent Record at line 354. Resolution path: PM/architect awareness only — confirm the change is acceptable and update the architecture's tsconfig snippet (if any) to include `resolveJsonModule`. No code action required.

## Deferred from: code review of story-1.4 (2026-05-09)

- **LOW — `BUNDLE_FORMAT_VERSION` constant is exported but the schema validates `version: z.string()` without cross-checking against it.** Any string passes — including future `'2.0.0'` bundles that the parser is not equipped to handle. Story 1.4 spec explicitly defers cross-version handling to Story 1.6 (`bundle-parser.ts`), where `BUNDLE_VERSION_MISMATCH` is raised. Resolution path: Story 1.6 wires the constant into the parser's compatibility check; no Story 1.4 action.
- **LOW — `applySanitization` re-allocates a sanitized object even when `bundle.credentials.wasRedacted` is already `true`.** Idempotent in semantic effect, but wasteful. The function does not currently short-circuit re-redaction. Resolution path: the first caller that batches sanitization (Story 2.2 / Story 4.1) can add a `wasRedacted === true` short-circuit; not worth the test-surface change in Story 1.4.
- **LOW — `Bundle` schema accepts empty `projects: []` and empty `global: {}`.** A valid-by-schema bundle with zero projects passes parse. Story 1.4 ACs do not require a non-empty constraint — fixtures `invalid-missing-field.json` and `invalid-extra-field.json` deliberately use empty arrays/objects. Resolution path: if downstream commands need a non-empty constraint (e.g., `EXPORT_NOTHING_SELECTED` raising on an empty bundle export), enforce it at the command boundary in Stories 2.1+, not in the schema.
- **LOW — `applySanitization`'s shallow-copy preserves identity of `projects` / `global` references with the input bundle.** `sanitization-rules.test.ts:39-40` asserts `sanitized.projects === bundle.projects`. A future caller mutating `sanitized.projects[0]` would also mutate the source bundle. Acceptable for the immutable-bundle convention currently in force. Resolution path: if mutation-safety becomes a requirement, switch to a deep clone or freeze the result; revisit when the first command writes back into a sanitized bundle.

## Deferred from: code review of story-1.6 (2026-05-09)

- **LOW — `gunzipSync` has no `maxOutputLength` set; gzip-bomb risk on untrusted bundles.** A tiny gzip stream can decompress to GB and OOM the process. Story 1.6 ACs do not require resource-bounded decompression; cmemmov reads local files (low attacker-control surface). Resolution path: in a hardening pass (or when bundle-import lands in Stories 2.x for shared bundles from the network), call `gunzipSync(bytes, { maxOutputLength: <cap> })` and convert the resulting `RangeError` into a `CmemmovError({ code: 'BUNDLE_INVALID_SCHEMA', hint: 'bundle exceeds max decompressed size' })`. Add a fixture/test using a known gzip-bomb pattern.
- **LOW — Multi-issue Zod errors collapse to first issue.** `parseBundle` reports only `cause.issues[0]?.message` in the hint. If a malformed bundle has several validation problems (e.g., wrong type AND extra field AND missing field), only one is surfaced — fix-and-retry workflow is slower for users. Resolution path: when the Output module's structured-error rendering is wired up (Story 1.10+), have it consume `error.cause.issues` directly and render all issues. The parser's hint can stay terse; the UI layer handles aggregation.
- **LOW — Hex case-sensitivity on the `integrity` checksum comparison.** `createHash('sha256').digest('hex')` returns lower-case; the comparison at line 50 is case-sensitive. A future tool emitting upper-case hex (e.g., a third-party signer) would fail integrity check on otherwise-valid bundles. Resolution path: lower-case the comparison (`bundle.integrity.toLowerCase() === computed`). Defer until a real interop case appears — premature today.
- **LOW — Static `older-bundle-version.cmemmov` checksum is fragile to BundleSchema field-order changes.** The fixture's stored `integrity` was computed against the V8 insertion order produced by Zod's current schema definition. Reordering keys in `BundleSchema` would silently invalidate the fixture's checksum (test would fail with a misleading "corrupt" message). Resolution path: regenerate fixtures via a `beforeAll` helper (already discussed in story Dev Notes) that uses `serializeBundle` itself, OR add a `tests/fixtures/bundles/_generate.ts` script kept in-tree (Story 1.6 deleted it after one-shot use). Best paired with the first story that touches the schema.
- **LOW — Memory: large-bundle serialization holds three full copies in RAM simultaneously** (compact canonical, indented JSON, gzip output). For a 100MB bundle this is ~300MB peak. Story 1.6 does not target streaming. Resolution path: when streaming bundle I/O is on the roadmap (NOT currently in any epic), switch to a streaming canonicalizer + streaming gzip via `zlib.createGzip()`. No action this sprint.
- **LOW — `BUNDLE_VERSION_MISMATCH` error code defined but unused.** `src/core/error.ts:5` declares `BUNDLE_VERSION_MISMATCH` with `exitCode: 2`, but Story 1.6 AC4 explicitly downgrades version mismatch to a warning — so no path raises this code. Resolution path: either remove the code in a Story 1.4 cleanup, or repurpose for a stricter `--strict-version` opt-in flag in a future import command. Tracking only — no immediate action.

## Deferred from: code review of story-1.7 (2026-05-09)

- **MEDIUM — `deepMerge` Set-dedup applies uniformly to every array, but Story Dev Notes only specify string-array dedup for permission-rule arrays.** Object arrays (e.g., hook arrays in `settings.json`) get reference-based dedup which produces duplicates rather than logical-equality dedup, since `new Set([objA, objB])` retains both even when their fields are identical. Resolution path: introduce a per-key dedup strategy table (string arrays → Set, object arrays → identity-by-key, or wholesale replace) when Story 2.x merges land richer settings shapes. Or accept duplicates and let users clean up — they're cosmetic, not corrupting.
- **MEDIUM — Corrupt `settings.json` / `plugins.json` is silently treated as `{}` by `applyGlobalSettings` / `applyMcpConfig` / `applyPlugins`.** `safeParseJson` returns `undefined` on parse failure, the writer treats that as an empty object, deep-merges with the incoming data, and writes — destroying the prior content. Mitigated in practice by the import-time backup invariant (rollback recovers the corrupt-but-original file), but the writer should refuse to write when prior state can't be reasoned about. Resolution path: `applySettingsAt` / `applyMcpConfig` / `applyPlugins` should distinguish "absent" (ENOENT) from "malformed" (parse failure) and throw `CmemmovError({ code: 'INTERNAL', hint: 'settings.json is malformed; restore from backup or fix manually' })` for the latter. Add a regression test using a corrupt-JSON fixture.
- **MEDIUM — `applyPlugins` always writes `plugins.json` even when the existing installation uses the `plugins/<name>/config.json` directory form.** The reader (`readPlugins`) prefers `plugins.json` over the dir, so once `plugins.json` is written, subsequent reads silently mask the directory contents. Story Task 9 line 157 mentioned "same pattern as customCommands/teams depending on format found on disk" but Epic 1's writer always picks the JSON file form. Resolution path: a follow-up plugins-format clarification story decides whether the writer detects existing format and routes accordingly, or whether `plugins.json` is the canonical form going forward and the dir form is migrated.
- **MEDIUM — `applyTeams` merge has no defined behavior for incoming team configs lacking an `id` field.** Currently always written (no merge key → no collision detection). Resolution path: spec clarification — either reject id-less configs at the writer, or fall back to dir-name-based dedup, or always write (current behavior). Add a test case once decided.
- **LOW — `resolveOriginalPath` issues unbounded `Promise.all(stat)` over every JSONL in a project's sessions dir.** No concurrency cap. Acceptable for typical Claude installs (tens of sessions) but could hit EMFILE on very large session histories. Resolution path: introduce a small concurrency limiter (e.g., `p-limit` style) when sessions counts grow problematic, or short-circuit by streaming the dir entries and stopping at the first jsonl whose mtime is newer than a sliding cap. Defer until a real perf complaint surfaces.

## Deferred from: code review of story-1.8 (2026-05-09)

- **LOW — `decision-schema.test.ts` runtime `satisfies` assertion is an identity tautology.** Line 36-37 captures `ALL_CATEGORIES satisfies readonly ClaudeCategory[]` into `_check`, then asserts `expect(_check).toBe(ALL_CATEGORIES)`. The `satisfies` operator has no runtime effect — `_check` IS `ALL_CATEGORIES` by reference, so the runtime assertion always passes. Type-level guarantee is provided at compile time; the runtime line adds no coverage. Resolution path: when the test file is next touched, replace with a plain compile-time `// @ts-expect-error` negative test or delete the redundant runtime check. No correctness impact.
- **LOW — `output.ts` `finish()` is not idempotent.** No guard prevents double-call, so a buggy command path that calls `finish()` twice would write to stdout twice (violating AC1's "exactly one final stdout write per command" property). Current consumer scope (none — UI scaffold for future commands) cannot trigger this. Resolution path: when the first command (Story 2.x) wires up `Output`, add a `#finished` guard that no-ops on subsequent calls and emits a `warn` log; or accept the documented usage contract and let the linter catch double-calls in command code review.
- **LOW — `output.test.ts:166` `JSON.parse(raw)` throw assertion is value-fragile.** The "human mode emits raw summary, not JSON" test asserts `JSON.parse('hi\n')` throws. The assertion would pass for the chosen value `'hi'`, but if a future maintainer changes the summary to `'true'`, `'42'`, `'null'`, or any valid JSON literal, the test would silently fail to detect a regression. Resolution path: assert positively that the raw output equals `summary + '\n'` rather than asserting JSON.parse throws. Defer since the current test is correct for the chosen literal.
- **LOW — Unicode `⚠` warning prefix may render as `?` on non-UTF-8 Windows terminals.** `output.ts:36` uses `pc.yellow('⚠ ' + msg)`. Windows code page 1252 (legacy CMD default) cannot render U+26A0; modern Windows Terminal / PowerShell with `chcp 65001` are fine. Resolution path: when CI runs the cross-OS matrix (Story 1.13), capture stderr on Windows and verify the rendering; if affected, add a `picocolors`-style ANSI fallback or guard via `process.stdout.isTTY && process.platform === 'win32'`.
- **LOW — `selectProjects` (`src/ui/prompts.ts:83-93`) has no `silent` parameter.** Unlike its sibling prompts (`selectCategories`, `selectMergeMode`, `confirmCredentials`, `confirmOverwrite`) which all branch on `opts.silent`, `selectProjects` always falls through to `clack.multiselect` when `opts.options.length > 0`. A caller in silent mode that forgets to pre-filter would block waiting for stdin. Today the only consumer (`src/commands/export.ts`) gates the call behind a non-silent check, so the gap is not exploitable. Resolution path: when `--projects` selection lands as a first-class silent flag (any future export-targeting story), unify the API surface — accept `{ silent: boolean; value?: string[]; options: ProjectOption[] }` and follow the same require/return/throw pattern as the other prompts. Defer; no current behavioral defect.

## Deferred from: code review of story-1-8-cr-pass-2 (2026-05-09)

- **REVIEW NOTE — Re-review of Story 1.8 (cr-1-8 pass) found one MEDIUM and one new LOW.** The MEDIUM (dead `export type { ClaudeCategory }` re-export in `src/services/claude-writer.ts`) was patched in this pass; the LOW (`selectProjects` silent-mode parity gap) is logged immediately above. All four pre-existing deferred LOW items from the dev's first review pass remain accurate and are unchanged.

## Deferred from: code review of story-1.9 (2026-05-09)

- **HIGH — `package.json:files` whitelist excludes the dynamic-import chunks emitted by Story 1.9.** Story 1.1 AC3 mandated the verbatim whitelist `["dist/cmemmov.js", "dist/cmemmov.d.ts", "README.md", "LICENSE"]`. Story 1.9's lazy-import design causes `tsup` to emit additional chunks (`dist/export-<hash>.js`, `dist/import-<hash>.js`, …, `dist/chunk-<hash>.js`) that resolve via dynamic `import('./commands/*.js')` calls in `cli.ts`. After `npm publish` the chunks are missing from the tarball, so any non-help/non-version command invocation on an installed package will fail with `ERR_MODULE_NOT_FOUND`. Resolution path: Story 5.x (release prep / packaging) widens the `files` array — likely to `["dist/**/*.js", "dist/**/*.d.ts", "README.md", "LICENSE"]` — and adds an `npm pack` smoke test that runs `cmemmov --version` and `cmemmov export` (expecting INTERNAL/not-implemented through Epic 2, then real exports later) from a freshly extracted tarball. Until then, local `node dist/cmemmov.js …` works because all chunks are present in the build output directory.
- **LOW — Windows-specific `import.meta.url` self-execute guard may fail when `cmemmov` is installed globally.** `cli.ts` line ~96-99 compares `process.argv[1]` to `fileURLToPath(import.meta.url)` to decide whether to auto-invoke `main()`. On Windows, `process.argv[1]` is the canonical path produced by the npm-generated shim, while `fileURLToPath(import.meta.url)` reflects the path used to load the ESM module. Drive-letter case (`C:\` vs `c:\`) and 8.3 short-name expansion can cause string inequality even when the two paths refer to the same file. Today's smoke tests with `node dist/cmemmov.js` work because `process.argv[1]` matches `import.meta.url` exactly. Resolution path: couple with Story 5.x packaging tests — switch to a normalized comparison (`path.normalize` + `String.prototype.toLowerCase` on Windows) or use the more robust `import.meta.main` if Node ships it before the release.

## Deferred from: code review of story-1.10 (2026-05-09)

- **LOW — `writeFile` overwrites existing bundle path silently with no `--force` confirmation.** If `claude-export-2026-05-09.cmemmov` (or any user-supplied `--output` path) already exists, it is overwritten without warning. AC6 specifies "a single .cmemmov JSON bundle file is produced... at the path supplied by --output OR at the default" — silent overwrite is consistent with the spec literal but typical CLI tools warn or require `--force`. Resolution path: a future hardening / UX story adds an existence check that prompts in interactive mode and requires `--force` in silent mode. Not blocking — matches the story spec as written.
- **LOW — `writeFile` is non-atomic (no tmp+rename pattern).** A killed process mid-write leaves a partial bundle on disk. Inconsistent with the WriteGate atomic-write discipline used for writes inside `~/.claude/`. Resolution path: when WriteGate's atomic semantics are extended to user-supplied output paths (likely Story 4.x or 5.x packaging), route the export bundle write through the same primitive. Output artifacts are arguably less critical than home-directory writes, so deferral is reasonable.
- **LOW — `writeFile` errors (`EISDIR`, `EACCES`, `EROFS`, missing parent dir) bubble as opaque `INTERNAL`.** The CLI top-level handler wraps as `INTERNAL` with no specific user guidance. Resolution path: `export.ts run()` can wrap the writeFile call in a try/catch that converts known errno codes to `CmemmovError({ code: 'INTERNAL', hint: 'output path is a directory' })` etc. Defer to a UX-polish pass; today's behavior is correct, just unhelpful.
- **LOW — `parseCategories('all,bogus')` short-circuits on `all` and silently drops `bogus`.** The unknown-alias validation only fires when `all` is absent. Inconsistent with the documented "Unknown aliases → throw" behavior. Resolution path: validate every token before the `all` short-circuit, or accept the short-circuit and document it. One-line fix; deferred because the user-visible behavior (full export) matches what `all` means.
- **LOW — `bundle.credentials.content === null` with `wasRedacted: false` when credentials file is unparseable.** The `wasRedacted: false` is misleading — it suggests credentials were preserved verbatim, but they were actually unparseable and replaced with null. Resolution path: distinguish "no credentials" / "unparseable credentials" / "redacted credentials" with a third state, or surface a stderr warning when the file is present but unparseable. Defer until import-side credential handling lands (Story 1.11+) to design the contract end-to-end.
- **LOW — `resolveOriginalPath` is called for ALL projects in `surface.projects` even when the user will select a subset.** Each call reads the most recent JSONL session. For a user with 50 projects but exporting one, this performs 49 unneeded file reads. Resolution path: lazy resolution — defer reads to interactive label rendering (already needs all) and silent-mode selected-only reads. Acceptable today since interactive multi-select needs all labels; defer until perf complaint.
- **LOW — AC8's "structured error naming the missing flag" uses generic `INTERNAL` code.** A dedicated `MISSING_REQUIRED_FLAG` (or similar) code would be more specific than `INTERNAL`. Matches the existing codebase pattern (no other commands have a dedicated code for this). Resolution path: a small refactor when error-code taxonomy is next revisited (likely Story 5.4 docs pass).

## Deferred from: code review of story-1.11 (2026-05-09)

- **LOW — `ImportDecision.categories: [...ALL_CATEGORIES]` is set but never read inside `import.ts`.** The Story 1.11 Dev Notes proposed an `ImportDecision` shape *without* `categories` (lines 127-135 of the story), but the existing decision-schema interface (from prior stories) still has the field, and the dev populated it with `[...ALL_CATEGORIES]` to satisfy structural typing. Import-time category gating is done by `bundle.global.X !== undefined` checks, not by consulting `decision.categories`. Story 1.11 explicitly defers `--categories` filtering on import to Epic 2 (line 418 of the story). Resolution path: Epic 2 either drops `categories` from `ImportDecision` (and updates the satisfies test in `decision-schema.test.ts`) or wires the field into a real `--categories` filter on import. No action this story.
- **LOW — Empty global memory array (`bundle.global.memories = []`) triggers an apply, while empty project memory array (`project.memories = []`) is skipped.** `applyGlobalCategories` checks `g.memories !== undefined`, but `applyProjectCategories` checks `project.memories !== undefined && project.memories.length > 0`. In merge mode the apply is a near no-op (existing files preserved, MEMORY.md rebuilt from existing). In overwrite mode the difference matters: a global empty array triggers a wholesale wipe of `~/.claude/memory/`, while a project empty array does not. Resolution path: align gating — either consistently skip empty arrays or always apply. Defer because (a) overwrite-with-empty-array is a legitimate "clear this" semantic the user might rely on, and (b) the import command never produces empty arrays from a normal export round-trip; only hand-crafted bundles hit this case. Pair with the first hardening story that revisits empty-collection semantics across the writer surface.
- **LOW — JSON-mode error path drops the partial-import summary text.** When `IMPORT_PARTIAL` throws, `import.ts` emits the human "Imported N categories... Skipped X projects..." summary via `out.progress` (stderr) before throwing. In JSON mode `out.progress` still writes to stderr, but `cli.ts`'s error path calls `out.finish('', false)` with an empty summary — so the JSON blob's `summary` field is empty even though useful information was emitted. Resolution path: when the IMPORT_PARTIAL throw propagates, populate the JSON blob's `summary` field with the same partial-success summary so JSON consumers don't have to re-derive it from the `hint`. Defer — JSON-mode polish pass; current behavior is consistent with how every other command handles error-summary-in-JSON.
- **LOW — `confirmProjectPath` lists `skip` last in the interactive select prompt; story Dev Notes line 173 implied skip-first.** The story has two pseudo-listings of the option order: line 173 (text description) starts with "Always include: skip" first, while line 192-198 (sample TypeScript) lists `accept, override, skip`. The dev followed the sample; UX-defensibly `skip` last avoids accidental skips on Enter when `accept` is the highlighted default. Resolution path: spec clarification — confirm the intended ordering with PM/UX once an interactive QA pass is run on Story 1.11 in real terminals (the unit tests assert presence, not order). No code action pending the decision.
- **LOW — AC10 (rollback availability after partial failure) has no explicit unit test.** The story's Dev Agent Record acknowledges this and points to Story 1.12's end-to-end rollback test as the authoritative coverage. The invariant (no orchestration try/catch in `import.ts`; backup persists when an apply throws) is structural and indirectly verified by AC9's "no backup if checksum fails" test. Resolution path: Story 1.12 (rollback) adds an integration test that imports a deliberately-failing bundle, confirms `cmemmov rollback` finds the auto-backup, and restores cleanly. Tracking only.

## Deferred from: code review (re-review) of story-1.11 (2026-05-09)

- **MEDIUM (structural) — `bundle.global.claudeJson` is bundled by export but has no writer category to restore it.** Story 1.11 inherits this gap from Stories 1.7-1.10. The export pipeline (`commands/export-selection.ts:159-160`) writes `bundle.global.claudeJson` from `surface.claudeJson`, and the `BundleSchema` declares the field, but `claude-writer.ts::ApplyCategoryOpts` has no `claudeJson` member, `decision-schema.ts::ClaudeCategory` does not include `claudeJson`, and `import.ts::applyGlobalCategories` does not branch on it. Consequence: a same-OS export → import round-trip silently drops `~/.claude.json` content (project history, MCP server registry, first-start metadata). AC11 of Story 1.11 implies `.claude.json` IS processed (just without path remapping), so this is a real gap in same-OS happy-path coverage. **Mitigation applied in this review:** `import.ts` now emits `out.warn()` when `bundle.global.claudeJson !== undefined` so users know it is being skipped. Resolution path: a follow-up story extends `ClaudeCategory` with `claudeJson`, adds an `applyCategory({ category: 'claudeJson', data: unknown, ... })` branch in `claude-writer.ts` (semantics: deep-merge for `merge` mode, wholesale replace for `overwrite` mode, identical to `globalSettings`), wires it into `import.ts::applyGlobalCategories`, and adds round-trip integration coverage. Same story should also decide whether `claudeJson` is in `ALL_CATEGORIES` for `--categories` filtering.
- **LOW — `g.teams !== undefined && isRecord(g.teams)` silently skips when `teams` is non-record.** `bundle.global.teams` is typed as `unknown` by the schema (`bundle-schema.ts:35`). If a malformed or future-format bundle ships `teams` as an array, primitive, or null, the `isRecord` guard short-circuits with no progress line and no warning — the user sees `Applied plugins` but never knows `teams` was dropped. Resolution path: when the import command revisits its category dispatch (likely Story 2.x or a hardening pass), either tighten the schema to `z.record(z.unknown()).optional()` (rejects non-records at parse time with a clear error) OR emit `out.warn('Bundle teams field is not a record; skipping.')` when the guard fails. One-line fix; deferred because today's behavior is silent-but-correct on the writer side.
- **LOW — `parseMode('overwrite=')` (empty category) error message has trailing whitespace.** `import.ts:43-49` builds the hint as `Unknown category in --mode: ${catName}`. When `catName === ''` the user sees `Unknown category in --mode: ` (trailing space, no value shown). Functionally correct (rejects the bad input), but UX-unhelpful. Resolution path: short-circuit empty `catName` with a dedicated message: `--mode requires a category after '=' (e.g., overwrite=globalSettings)`. Trivial; defer to next time the file is touched.
- **LOW — `ImportOpts.json` flag is not tested for forwarding.** `buildDecision` reads `opts.json === true` and propagates it to `Output('import', { json })`. There is no test covering the JSON-mode summary structure for import (other commands like rollback have one). Resolution path: add a regression test asserting that `run('/tmp/bundle.cmemmov', { json: true })` produces a JSON summary on stdout matching the schema `{ success: true, command: 'import', summary, errors: [], warnings }`. Best paired with the JSON-mode polish entry already deferred from the original review (partial-import summary text dropped in JSON mode).

## Deferred from: code review of story-1-12-rollback-command (2026-05-09)

- **MEDIUM — AC5 only detects empty-directory corruption, not truncated files or missing required paths inside a non-empty backup.** AC5 verbatim: "the most-recent backup directory is partially corrupted (truncated file, missing required path) → a clean error explains the corruption; rollback does NOT silently fall back to the next-most-recent backup." The Story 1.12 implementation throws `ROLLBACK_NOT_AVAILABLE` for ENOENT-on-readdir and zero-entry directories, and (post-review) for unreadable backup files via `access()`. It still does NOT detect a backup file that exists, opens, and reads cleanly but contains truncated content (e.g., a `.claude.json` of 0 bytes that should contain a JSON object). The "missing required path" half of AC5 is also undefined operationally — there is no manifest of what a backup must contain. Resolution path: backup-service.ts emits a `_manifest.json` per backup containing per-file SHA-256 + byte length; rollback.ts validates each restored file against the manifest before clearing `~/.claude/`. Pair with the first hardening story that revisits backup integrity (likely Story 5.x release prep or a dedicated "backup hardening" follow-up).
- **LOW — `backup-service.ts` does not emit an integrity manifest.** Same architectural gap as the AC5 entry above, framed from the producer side. The current backup is a plain directory copy; there is no signed/checksummed proof that the backup represents a coherent snapshot. Resolution path: same as above — manifest emission in `backup-service.ts` is the prerequisite for true AC5 compliance.
- **LOW — Dry-run still calls `readFile` on every backup file.** `rollback.ts` reads each file's bytes through the gate so the recorded `write` op contains real `bytes` counts (matching production output reporting). For multi-GB backups (large session histories) this is wasteful read I/O on a `--dry-run` invocation. Resolution path: in dry-run mode, swap to `stat()`-based size lookup and pass a placeholder `Buffer.alloc(size)` into `gate.write` so byte counts remain accurate without paging the actual content. Defer until a perf complaint surfaces.
- **LOW — Race window between `readdir(BACKUP_ROOT)` and `readdir(backupDir)`.** A second process could delete the chosen backup directory between the two calls. The user would then see the "appears empty or corrupted" hint, which misattributes the cause. Resolution path: collapse to a single readdir+stat sequence, or detect ENOENT on the second readdir specifically and adjust the hint. Very unlikely in practice; UX-only impact.
- **LOW — No CLI dispatch test verifies `--backup <path>` flag is forwarded through `optsWithGlobals`.** `cli.test.ts` AC8 only covers `cmemmov rollback` (no flags). Wiring of `--backup` from the command line into `run({ backup })` is exercised end-to-end only at install time. Resolution path: add a dispatch test that asserts `tracker.runImpl.rollback` receives `{ backup: '...' }` after invoking `runCli(['rollback', '--backup', '/some/path'])`. Trivial to add when the cli test file is next touched.
- **LOW — Coupled assumption between `rollback.ts` `.claude.json`-at-backup-root logic and `backup-service.ts` layout.** rollback.ts hardcodes `entry.relPath === '.claude.json'` to detect the adjacent `~/.claude.json` restore target. If `backup-service.ts` ever changes the placement (e.g., into a `_meta/` subdirectory), rollback silently misroutes the file as a child of `~/.claude/` instead of failing loudly. Resolution path: introduce a small contract test that asserts the round-trip (create-backup-of-known-shape → rollback → equal contents) and runs against the real services. Best paired with the backup-manifest work above.
- **LOW — Test fixture `recordedOpsResult` uses string-template path concatenation instead of `path.join`.** `rollback.test.ts:316-319` builds expected paths via `${CLAUDE_DIR}/settings.json` (forward slash hardcoded), which produces mixed-separator strings on Windows where `CLAUDE_DIR` is built with backslashes. The test still passes because it only asserts on `kind: 'write'` counts, not path values. Resolution path: switch to `path.join` for consistency with the other path fixtures in the same file. Trivial; defer to a tidy-up pass.

## Deferred from: code review of story-1-13-ci-matrix-3-os-test-pipeline (2026-05-09)

- **MEDIUM — Pre-existing canonical-key-order bug between `buildBundle` and `BundleSchema.parse` causes false `BUNDLE_CHECKSUM_MISMATCH` on round-trip when `global` carries multiple optional keys.** `src/commands/export-selection.ts::buildBundle` constructs the `Bundle` object in author key order (e.g., `globalSettings`, then `claudeMd`, then `claudeJson`); `BundleSchema.parse` in `src/services/bundle-parser.ts` re-orders keys to match Zod's schema-declaration order. `JSON.stringify` of the two produces different bytes → different SHA-256 → false integrity failure. Existing unit-test fixtures only carry one key under `global` so the bug never surfaced; the new integration tests (`tests/integration/network-shim.test.ts`, `tests/integration/perf.test.ts`, `tests/integration/large-perf.test.ts`) exercise multi-key `global` and tripped it. Story 1.13 is purely infrastructure (no `src/` changes), so the integration tests pass `integrityCheck: false` with inline comments at: `tests/integration/network-shim.test.ts:206-211`, `tests/integration/perf.test.ts:142-145`, `tests/integration/large-perf.test.ts:88-90`. Resolution path: a dedicated bug-fix story under Epic 1 cleanup. Likely fix: in `buildBundle`, return `BundleSchema.parse(bundle)` instead of the manual literal — guarantees output matches Zod's canonical key order. Add a unit test using a multi-key `global` fixture and `bundle-serializer → bundle-parser` round-trip without `integrityCheck: false`.
- **MEDIUM — AC12 (filesystem allowlist) is asserted by code review only, not by an automated test.** AC12 verbatim: "no file has been created or written outside (a) `~/.claude/backups/cmemmov/`, (b) the user-supplied `--output <path>` for `export`/`share`, (c) the test's own temp directory under `os.tmpdir()`, (d) the existing `~/.claude/` and `~/.claude.json` surfaces being managed". The integration tests (network-shim/perf/large-perf) all run inside isolated tmpdirs and so cannot mechanically verify "no writes outside the allowlist" — a regression that wrote into `process.cwd()` or `os.homedir()` would not be caught by any test in this story. Resolution path: a follow-up "filesystem allowlist verification" story adds an integration test that snapshots the contents of `process.cwd()` / `os.homedir()` before and after each command (or, more robustly, runs the command with `node:fs/promises` patched to log every write path and asserts each path falls inside the allowed set). Pair with the first hardening story that revisits CC1 enforcement.
- **LOW — `network-shim.test.ts` does not mock `node:http2`.** AC9 names `node:net`, `node:dgram`, `node:tls`, `node:https` — `node:http2` is out of spec scope. None of cmemmov's current dependencies use HTTP/2, but if a future dep does (e.g., a gRPC client), traffic would slip past the shim. Resolution path: add a `vi.mock('node:http2', ...)` block when AC9 is next revisited or when a dep introduces HTTP/2. Trivial extension of the existing pattern.
- **LOW — `large-sessions-builder.mjs` self-execute guard is fragile on Windows.** The guard compares `import.meta.url` against a `file://`-prefixed, slash-normalized form of `process.argv[1]`; on Windows that comparison can fail across drive-letter casing differences (`C:` vs `c:`) or 8.3 short-name expansion. Same root cause as the deferred entry from story-1.9 about `cli.ts`'s `import.meta.url` self-execute guard. The builder works correctly when imported by `large-perf.test.ts`; the CLI entrypoint is the only affected path. Resolution path: same as story-1.9 deferred entry — switch to `path.normalize` + lowercased comparison, or use `import.meta.main` if Node ships it.
- **LOW — `actions/setup-node@v4` is not pinned to a SHA.** `.github/workflows/ci.yml` and `.github/workflows/nightly.yml` use `uses: actions/setup-node@v4` (floating tag). Story Dev Notes explicitly accept this ("If strict SHA pinning is required ... separately"). Resolution path: a supply-chain hardening pass (likely Story 5.x release prep) replaces every `@v4` reference with a full SHA + version comment, and adds a `dependabot.yml` config for action updates.
- **LOW — `vitest.config.ts` per-file coverage threshold keys (`'src/core/path-engine.ts'`, etc.) are untested.** Coverage thresholds only fire when running `vitest run --coverage`, which is not currently in any CI job. If `@vitest/coverage-v8`'s path-normalization for per-file keys ever changes (or already differs from the documented form), the thresholds would silently no-op and AC6 would be unverified. Resolution path: either (a) add a `coverage` job to `ci.yml` running `npm run coverage:run` on Linux only, or (b) add a one-shot script that asserts each per-file threshold key resolves to a real coverage report entry. Option (a) is preferred but adds CI minutes; defer the cost decision to the maintainer.
- **LOW — Perf tests do not assert bundle file size > 0 / non-trivial.** `tests/integration/perf.test.ts` and `tests/integration/large-perf.test.ts` measure wall-clock time only; a regression that produced an empty bundle would also import quickly and pass both budgets. Resolution path: add `expect((await stat(bundlePath)).size).toBeGreaterThan(N)` after each export — N chosen relative to the fixture (e.g., > 1 KB for 20-project, > 100 MB for 500 MB sessions). Cheap insurance.

## Deferred from: code review of story-1.5 (2026-05-09)

- **MEDIUM — AC6 ESLint rule scope deviation: `src/**/*.test.ts` exempted in addition to the two service modules.** AC6 verbatim: "EXCEPT `src/services/write-gate.ts` and `src/services/backup-service.ts`." The dev added `src/**/*.test.ts` to the rule's `ignores` because tests need direct fs access for fixture setup and mock injection (`eslint.config.js:30`). Justification is documented in the story Completion Notes. Three independent test files in `src/core/` and `src/services/` already use `fs/promises` directly for setup. Resolution path: PM/Architect ratification — either confirm the exemption as intended (most likely; matches established project test pattern) and amend AC6 to explicitly allow `*.test.ts`, or split test fixtures into `tests/integration/` so the rule can apply strictly. No code action required pending the decision.
- **LOW-MEDIUM — AC4 strict-text deviation: `<claudeDir>/backups/**` is excluded from the recursive copy.** AC4 verbatim: "recursively copies the entire `claudeDir` tree." `stageClaudeCopy` filters out `<claudeDir>/backups` and any nested path under it (`backup-service.ts:18-25`) to prevent recursive backup-of-backups (combinatorial explosion). The deviation is necessary for correctness but is not in the spec. Resolution path: Architect updates AC4 (or the architecture's "Filesystem Discipline" section) to explicitly state that `backups/` is meta-state and excluded from new backups. No code action required.
- **LOW — backup-service.ts pruning may include non-backup entries that happen to live in `<claudeDir>/backups/cmemmov/`.** `readdir` returns ALL entries; `rm(..., { recursive: true })` removes them regardless of type. A stray `.DS_Store`, `Thumbs.db`, or accidental file in the backup root would be deleted. Resolution path: filter `entries` to only include directory names matching the timestamp pattern (`/^\d{4}-\d{2}-\d{2}T.*-\d+-[0-9a-f]{8}$/`) before pruning. Low risk because the directory is owned by cmemmov and shouldn't contain other entries; revisit if a user reports unexpected file deletion.
- **LOW — backup-service.ts cross-volume cp fallback can leak the staging directory.** If `rename(staging, backupDir)` rejects with EXDEV and the subsequent `cp(staging, backupDir, { recursive: true })` then throws (e.g., disk full mid-copy), `rm(staging, ...)` is not called and the staging dir orphans in `os.tmpdir()` (`backup-service.ts:50-52`). The OS cleans tmpdir periodically so this is bounded but not zero. Resolution path: wrap the cp call in try/finally so the staging dir is removed regardless of cp's outcome. Defer to a hardening pass — not a Story 1.5 blocker.
- **LOW — `recordedOps()` returns the live internal array reference.** The TypeScript `readonly WriteOp[]` annotation is compile-time only; callers can `as WriteOp[]` to mutate, breaking the gate's invariant that ops are immutable post-recording. Resolution path: return `Object.freeze([...ops])` or a `structuredClone(ops)` per call. Cost: per-call array copy. Defer until a real consumer demands runtime immutability — internal-only API for now.
- **LOW — `backup-service.test.ts:89-109` "does NOT prune when the new backup write throws" exercises an early-throw path (non-existent claudeDir → mkdtemp/cp throws), not a mid-function throw after the new backup is partially written.** The test demonstrates the property in spirit ("any throw before pruning short-circuits it"), but the trickiest variant — backup-write throws AFTER the rename but before pruning — is uncovered. Resolution path: add a fault-injection test using `vi.mock('node:fs/promises')` to force a mid-function throw on `readdir`, asserting that the just-created backup remains and prior entries are intact. Defer to a hardening pass.
